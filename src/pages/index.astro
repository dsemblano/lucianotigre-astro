---
import BaseLayout from '../layouts/BaseLayout.astro'
import { SITE_BASE, SocialLinks } from '../consts'
import { Icon } from 'astro-icon/components'
import BlogCard from '../components/BlogCard.astro'
import { getCollection } from 'astro:content'

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 4)

// const images = ['/hero2.jpg', '/hero3.jpg']
// const randomImage = images[Math.floor(Math.random() * images.length)]

// import hero2 from '../assets/hero2.jpg'
// import hero3 from '../assets/hero3.jpg'

// const images = [hero2.src, hero3.src]
// const randomImage = images[Math.floor(Math.random() * images.length)]
---

<style>
	.bg-hero {
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
	}
</style>

<script>
	const images = ['/hero2.jpg', '/hero3.jpg'] // from /public
	const randomImage = images[Math.floor(Math.random() * images.length)]

	const hero = document.getElementById('hero')
	hero.style.backgroundImage = `
    linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)),
    url(${randomImage})
  `
</script>

<BaseLayout>
	<div id="hero" class="bg-hero bg-yellow-400 w-full">
		<main
			class="app-container flex flex-col gap-12 justify-center py-36 md:py-48 pt-44 md:pt-56 relative"
		>
			<section class="hero-section text-white">
				<p class="block text-3xl opacity-70 mt-9 mb-4">
					Bushcraft e sobrevivência
				</p>
				<h1 class="text-6xl lg:text-8xl font-bold leading-none">
					Luciano Tigre
				</h1>
				<p
					class="text-sm lg:text-xl max-w-2xl font-light leading-normal mt-6 mb-2"
				>
					Sobrevivência | Selva | Mar | Deserto | Gelo
				</p>
				<p class="text-xl lg:text-4xl max-w-2xl font-bold leading-normal mt-6">
					Em breve a loja de produtos!
				</p>
				<!-- <div class="flex flex-wrap gap-8">
					{
						SocialLinks.map((link) => (
							<a
								class="text-xl text-primary dark:text-primary-dark underline hover:underline underline-offset-4"
								href={link.url}
								target="_blank"
							>
								{link.name}
							</a>
						))
					}
				</div> -->
			</section>
		</main>
	</div>

	<!-- Recent Posts Section -->
	<section class="app-container py-42 grid lg:grid-cols-2 gap-28 lg:gap-10">
		<div class="space-y-6">
			<h2 class="section-title">Sobre mim</h2>
			<div class="flex flex-col lg:flex-row gap-4">
				<p class="text-lg max-w-xl pb-4">
					Luciano Tigre é apresentador do Discovery Channel e da nova temporada
					de Desafio em Dose Dupla Brasil técnicas de sobrevivência Piloto de
					avião, Professor e instrutor de sobrevivência na Selva para os Cursos
					de Comissário de bordo e Pilotos de aeronaves em Curitiba - Drakon
					escola de aviação Civil e Aeroporto do Bacacheri. Também professor de
					meteorologia aeronáutica para o curso de Pilotos de avião e
					helicóptero.
				</p>
				<picture>
					<img class="rounded-2xl" src="aboutme.webp" alt="" />
				</picture>
			</div>
			<a class="btn-secondary" href={`${SITE_BASE}/about`}
				>Leia mais
				<Icon size={20} name="mdi:arrow-right" />
			</a>
		</div>
		<div class="space-y-6">
			<h2 class="section-title">Contato cursos e eventos</h2>
			<p class="text-lg max-w-xl">
				Use the buttons below for my resume & email.
			</p>
			<div class="flex flex-wrap mt-auto space-x-4">
				<a
					class="btn-primary"
					href="mailto:example@email.com"
					download
					target="_blank"
					>Get in touch
					<Icon height={20} name="mdi:email" />
				</a>
				<a class="btn-secondary" href="https://example.com" target="_blank"
					>Download Resume
					<Icon height={20} name="mdi:arrow-collapse-down" />
				</a>
			</div>
		</div>
	</section>

	<!-- Recent Posts Section -->
	<section class="app-container">
		<h2 class="section-title">Recent Posts</h2>
		<div class="posts-grid-container">
			{posts.map((post) => <BlogCard post={post} />)}
		</div>
		<a class="btn-secondary" href={`${SITE_BASE}/blog`}
			>Read All
			<Icon size={20} name="mdi:arrow-right" />
		</a>
	</section>
</BaseLayout>
